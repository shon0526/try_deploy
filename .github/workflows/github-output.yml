name: GITHUB_OUTPUT
on: push
jobs:
  share:
    runs-on: ubuntu-24.04
    steps:
      - id: source
        run: echo "result=Hello" >> "${GITHUB_OUTPUT}"
      - env:
          RESULT: ${{ steps.source.outputs.result }}
        run: echo "${RESULT}"
